# Traffic Sentinel - Minimal Requirements (Data-Efficient)
# For Fedora 42 with limited mobile data (\~150-190 MB total download)

# Core scientific computing
numpy>=1.24.0
pandas>=2.0.0

# Computer Vision (Essential)
opencv-python-headless>=4.8.0
Pillow>=10.0.0

# YOLO Vehicle Detection (Main ML engine)
ultralytics>=8.2.0

# Utils
tqdm>=4.65.0
python-dotenv>=1.0.0
joblib>=1.3.0

# Backend (if you want the API)
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
pydantic>=2.0.0